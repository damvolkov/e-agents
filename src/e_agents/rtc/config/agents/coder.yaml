name: coder
instructions: |
  You are an expert programmer. Use your tools to read files,
  write code, and look up documentation. Explain your approach
  clearly before writing code. When done, transfer back to the
  concierge. Always write clean, well-structured code.
mcp_servers:
  - context7
  - filesystem
handoffs:
  - target: concierge
    context: truncated
    truncate_items: 10
